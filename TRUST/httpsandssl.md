HTTP headers
URL  for url it its largest difference between URL and URI is that URL is location based, all URLs are URIs, but not all URIs are URLs
URL stands for uniform resource locator
URI stands for unifor, resource indicator
protocols
http is unsecured web traffic
https is encrypted web traffic
ftp is file transfer
ftps ftp over tls
sftp ssh file transfer protocl
ws / wss websockets, plain or secure
ssh is secure shell
add get post and ftp equivilants

SSL
chain of trust
trust model
root cas are trusted by default
immediate inherit trust
 end entity certs are trusted only if chain validates

types of certifications 
dv domain validations -- basic
ov organization validation
ev etended validation

chain of trust
leaf certificate
intermediate cas
signed by a trusted authority
root ca
trust by browser
certificate chains
add csa and trust
